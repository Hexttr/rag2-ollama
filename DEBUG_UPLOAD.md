# 🔍 Отладка загрузки документов

## ✅ Исправлено

1. **Добавлено логирование** - теперь видно, что происходит
2. **Обработка ошибок** - ошибки отображаются пользователю
3. **Валидация файлов** - проверка типа и размера
4. **Установлены зависимости PageIndex** - tiktoken, pymupdf, PyPDF2, pyyaml

## 🔧 Как проверить

### 1. Проверьте консоль браузера (F12)
Должны быть логи:
- `File selected: ...`
- `Starting upload...`
- `Document uploaded successfully` или ошибка

### 2. Проверьте Network tab (F12 → Network)
- Найдите запрос к `/api/documents/upload`
- Проверьте Status (должен быть 200)
- Проверьте Response

### 3. Проверьте логи backend
В терминале, где запущен backend, должны быть:
- `Uploading file: ...`
- `File saved to: ...`
- `Document created with ID: ...`

## 🐛 Если все еще не работает

### Проверьте:
1. **Backend запущен?**
   ```bash
   curl http://localhost:8000/api/health
   ```

2. **База данных работает?**
   ```bash
   cd backend
   python check_errors.py
   ```

3. **Зависимости установлены?**
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

4. **Директории созданы?**
   - `backend/uploads/` должна существовать
   - `backend/indices/` должна существовать

## 📝 Логи для отладки

Откройте консоль браузера (F12) и посмотрите:
- Есть ли ошибки в красном цвете?
- Что показывает Network tab?
- Какой статус у запроса?

---

*Попробуйте загрузить документ снова и проверьте логи!*






