# 🚀 Быстрый старт

## ✅ Статус: Приложение запущено и готово к тестированию!

### Сервер работает на: http://localhost:8000

## Быстрые команды для тестирования

### 1. Проверка здоровья сервера
```bash
curl http://localhost:8000/api/health/
```

### 2. Проверка Ollama
```bash
curl http://localhost:8000/api/health/ollama
```

### 3. Список документов
```bash
curl http://localhost:8000/api/documents/
```

### 4. Загрузка PDF документа
```bash
curl -X POST http://localhost:8000/api/documents/upload -F "file=@путь/к/файлу.pdf"
```

**Пример с существующим файлом:**
```bash
curl -X POST http://localhost:8000/api/documents/upload -F "file=@backend/uploads/Haval F7 instruction.pdf"
```

### 5. Проверка статуса индексации
```bash
curl http://localhost:8000/api/documents/{document_id}
```

## Автоматическое тестирование

Запустите готовый тестовый скрипт:

```bash
cd backend
python test_api.py
```

## Что исправлено

✅ Патчинг PageIndex для работы с Ollama  
✅ API для загрузки и индексации документов  
✅ Фоновая индексация документов  
✅ Обработка ошибок и логирование  

## Важно

- Убедитесь, что Ollama запущен: `ollama serve`
- Модель по умолчанию: `llama3.1:8b` (можно изменить в `backend/app/core/config.py`)
- Логи находятся в: `backend/logs/backend.log`

## Следующие шаги

1. Загрузите PDF документ через API
2. Дождитесь завершения индексации (статус `ready`)
3. Используйте API чата для вопросов по документу

Подробная документация: см. `TESTING_README.md`
