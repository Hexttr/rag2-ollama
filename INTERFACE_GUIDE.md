# 🖥️ Интерфейсы PageIndex

## Обзор интерфейсов

PageIndex предоставляет **несколько способов взаимодействия**, в зависимости от ваших потребностей:

---

## 1. 📟 CLI (Command Line Interface) - Командная строка

### Что это?
**Текстовый интерфейс командной строки** - основной способ использования открытого исходного кода.

### Как выглядит?

```bash
# Базовое использование
python run_pageindex.py --pdf_path document.pdf

# С параметрами
python run_pageindex.py \
    --pdf_path document.pdf \
    --model gpt-4o-2024-11-20 \
    --max-pages-per-node 10 \
    --if-add-node-summary yes

# Для Markdown
python run_pageindex.py --md_path document.md
```

### Пример вывода:

```
Parsing PDF...
start detect_page_index
start toc_transformer
start toc_index_extractor
Parsing done, saving to file...
Tree structure saved to: ./results/document_structure.json
```

### Особенности:
- ✅ **Простой** - только текст, без графики
- ✅ **Быстрый** - для автоматизации
- ✅ **Гибкий** - много параметров настройки
- ❌ **Нет визуализации** - результаты в JSON файле
- ❌ **Только индексация** - нет интерактивного поиска

### Когда использовать:
- Автоматизация обработки документов
- Интеграция в скрипты
- Пакетная обработка
- Серверные приложения

---

## 2. 🌐 Cloud Chat Platform - Веб-интерфейс

### Что это?
**ChatGPT-стиль веб-платформа** для работы с документами.

**URL:** https://chat.pageindex.ai

### Как выглядит?

```
┌─────────────────────────────────────────┐
│  PageIndex Chat                          │
├─────────────────────────────────────────┤
│                                          │
│  📄 Загрузить документ                  │
│  [Выбрать файл] [Загрузить]             │
│                                          │
│  💬 Чат с документом:                   │
│                                          │
│  👤 Вы: Каковы основные риски?           │
│                                          │
│  🤖 AI: Основные риски включают...      │
│     📍 Источник: Раздел 3.2 (стр. 15-18)│
│                                          │
│  [Введите вопрос...]                    │
└─────────────────────────────────────────┘
```

### Особенности:
- ✅ **Визуальный интерфейс** - удобно для пользователей
- ✅ **Интерактивный чат** - задавайте вопросы
- ✅ **Множественные документы** - работа с несколькими файлами
- ✅ **Источники** - показывает, откуда взята информация
- ❌ **Требует регистрацию** - нужен API ключ
- ❌ **Платный** - облачный сервис

### Когда использовать:
- Для конечных пользователей
- Быстрое тестирование
- Демонстрации
- Когда нужен удобный интерфейс

---

## 3. 📚 Jupyter Notebooks - Интерактивные примеры

### Что это?
**Интерактивные блокноты** с примерами использования.

### Расположение:
- `cookbook/pageindex_RAG_simple.ipynb` - простой RAG
- `cookbook/vision_RAG_pageindex.ipynb` - vision-based RAG
- `cookbook/agentic_retrieval.ipynb` - агентный поиск

### Как выглядит?

```python
# Ячейка 1: Установка
%pip install pageindex

# Ячейка 2: Настройка
from pageindex import PageIndexClient
pi_client = PageIndexClient(api_key="YOUR_KEY")

# Ячейка 3: Загрузка документа
doc_id = pi_client.submit_document("document.pdf")["doc_id"]

# Ячейка 4: Поиск
results = pi_client.search(doc_id, query="Каковы риски?")

# Ячейка 5: Результаты
print(results)
```

### Особенности:
- ✅ **Интерактивный** - можно экспериментировать
- ✅ **Визуализация** - графики, таблицы
- ✅ **Обучение** - пошаговые примеры
- ✅ **Документация** - объяснения встроены
- ❌ **Требует Jupyter** - нужна установка

### Когда использовать:
- Изучение PageIndex
- Эксперименты
- Обучение
- Прототипирование

---

## 4. 🔌 API - Программный интерфейс

### Что это?
**REST API** для интеграции в приложения.

### Пример использования:

```python
from pageindex import PageIndexClient

# Инициализация
client = PageIndexClient(api_key="YOUR_API_KEY")

# Загрузка документа
doc_id = client.submit_document("document.pdf")["doc_id"]

# Поиск
results = client.search(
    doc_id=doc_id,
    query="Каковы основные риски компании?",
    max_results=5
)

# Получение ответа
answer = client.generate_answer(
    doc_id=doc_id,
    query="Каковы основные риски компании?",
    context=results
)
```

### Особенности:
- ✅ **Гибкость** - интеграция в любые приложения
- ✅ **Автоматизация** - программный доступ
- ✅ **Масштабируемость** - для продакшена
- ❌ **Требует программирования** - нужны навыки кода
- ❌ **Платный** - облачный API

### Когда использовать:
- Интеграция в приложения
- Автоматизация процессов
- Масштабное использование
- Кастомные решения

---

## 5. 🐍 Python SDK - Библиотека

### Что это?
**Python библиотека** для использования в коде.

### Пример использования:

```python
from pageindex import page_index_main, config

# Настройка
opt = config(
    model="gpt-4o-2024-11-20",
    if_add_node_summary="yes"
)

# Индексация
tree = page_index_main("document.pdf", opt)

# Работа с деревом
for node in tree['structure']:
    print(f"{node['title']}: {node['summary']}")
```

### Особенности:
- ✅ **Полный контроль** - доступ ко всем функциям
- ✅ **Локальное использование** - можно с Ollama
- ✅ **Кастомизация** - модификация под нужды
- ❌ **Требует программирования** - нужны навыки
- ❌ **Нет UI** - только код

### Когда использовать:
- Разработка приложений
- Кастомные решения
- Исследования
- Интеграция в существующие системы

---

## 6. 🔗 MCP (Model Context Protocol)

### Что это?
**Интеграция с Claude, Cursor** и другими MCP-совместимыми агентами.

### Как использовать:

1. Настроить MCP сервер PageIndex
2. Подключить к Claude Desktop или Cursor
3. Использовать через чат-интерфейс агента

### Особенности:
- ✅ **Интеграция с AI агентами** - Claude, Cursor
- ✅ **Естественный интерфейс** - через чат
- ✅ **Мощные возможности** - комбинация с другими инструментами
- ❌ **Требует настройки** - не из коробки

### Когда использовать:
- Работа с Claude/Cursor
- Комплексные задачи
- Интеграция в рабочий процесс

---

## 📊 Сравнение интерфейсов

| Интерфейс | Сложность | Визуализация | Интерактивность | Стоимость |
|-----------|-----------|--------------|-----------------|-----------|
| **CLI** | ⭐ Легко | ❌ Нет | ❌ Нет | ✅ Бесплатно* |
| **Chat Platform** | ⭐ Легко | ✅ Да | ✅ Да | 💰 Платно |
| **Jupyter** | ⭐⭐ Средне | ✅ Да | ✅ Да | ✅ Бесплатно* |
| **API** | ⭐⭐⭐ Сложно | ❌ Нет | ⚠️ Через код | 💰 Платно |
| **SDK** | ⭐⭐⭐ Сложно | ❌ Нет | ⚠️ Через код | ✅ Бесплатно* |
| **MCP** | ⭐⭐ Средне | ✅ Да | ✅ Да | 💰 Платно |

*Бесплатно при использовании с Ollama или собственным OpenAI ключом

---

## 🎯 Какой интерфейс выбрать?

### Для начинающих:
1. **Chat Platform** - самый простой способ попробовать
2. **Jupyter Notebooks** - для изучения

### Для разработчиков:
1. **CLI** - для автоматизации
2. **SDK** - для интеграции
3. **API** - для продакшена

### Для конечных пользователей:
1. **Chat Platform** - готовое решение
2. **MCP** - интеграция с существующими инструментами

---

## 🚀 Быстрый старт по интерфейсам

### CLI:
```bash
python run_pageindex.py --pdf_path doc.pdf
```

### Chat Platform:
1. Откройте https://chat.pageindex.ai
2. Зарегистрируйтесь
3. Загрузите документ
4. Задавайте вопросы

### Jupyter:
```bash
jupyter notebook cookbook/pageindex_RAG_simple.ipynb
```

### SDK:
```python
from pageindex import page_index_main, config
tree = page_index_main("doc.pdf", config())
```

---

## 💡 Создание собственного интерфейса

Вы можете создать свой веб-интерфейс на основе SDK:

```python
# web_app.py (пример с Flask)
from flask import Flask, render_template, request
from pageindex import page_index_main, config

app = Flask(__name__)

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/process', methods=['POST'])
def process():
    file = request.files['pdf']
    tree = page_index_main(file, config())
    return jsonify(tree)

if __name__ == '__main__':
    app.run()
```

---

*Выберите интерфейс, который лучше всего подходит для ваших задач!*






