# 💰 Стоимость и использование LLM в PageIndex

## ❓ Используется ли LLM?

**ДА!** PageIndex **активно использует LLM** на всех этапах работы.

### Где используется LLM:

#### 1. **Индексация документа (создание дерева структуры)**

PageIndex делает **множественные вызовы LLM API** для каждого документа:

- ✅ **Извлечение оглавления** - анализ первых страниц для поиска TOC
- ✅ **Проверка страниц** - проверка, на какой странице начинается каждый раздел
- ✅ **Генерация summary** - создание краткого описания для каждого узла дерева
- ✅ **Генерация описания документа** - общее описание документа
- ✅ **Валидация структуры** - проверка правильности созданного дерева

**Количество вызовов API:**
- Для документа 100 страниц: ~20-50 вызовов
- Для документа 500 страниц: ~50-150 вызовов
- Зависит от сложности структуры документа

#### 2. **Поиск и извлечение информации (retrieval)**

- ✅ **Reasoning-based поиск** - LLM анализирует дерево и выбирает релевантные разделы
- ✅ **Генерация ответов** - создание ответа на основе найденного контекста

**Количество вызовов API:**
- Каждый поиск/вопрос: 1-3 вызова API

#### 3. **Vision-based RAG** (опционально)

- ✅ **Анализ изображений страниц** - для работы без OCR
- ✅ Использует мультимодальные модели (GPT-4.1 Vision)

---

## 💵 Стоимость использования

### ❌ НЕ бесплатно!

PageIndex требует **платный API ключ OpenAI**.

### Что нужно:

1. **OpenAI API ключ** (обязательно)
   - Регистрация на https://platform.openai.com
   - Добавление платежного метода
   - Новые пользователи получают ~$5 бесплатных кредитов

2. **Настройка в проекте:**
   ```bash
   # Создать .env файл
   echo "CHATGPT_API_KEY=sk-..." > .env
   ```

### Стоимость моделей OpenAI (примерная):

| Модель | Входные токены | Выходные токены |
|--------|---------------|-----------------|
| GPT-4o | $2.50 / 1M | $10.00 / 1M |
| GPT-4o-2024-11-20 | $2.50 / 1M | $10.00 / 1M |
| GPT-4.1 | ~$10-15 / 1M | ~$30-40 / 1M |
| GPT-3.5-turbo | $0.50 / 1M | $1.50 / 1M |

### Примерная стоимость обработки документов:

#### Индексация (один раз):

| Размер документа | Вызовов API | Примерная стоимость |
|-----------------|-------------|---------------------|
| 50 страниц | ~15-30 | $0.20 - $0.80 |
| 100 страниц | ~30-50 | $0.50 - $2.00 |
| 200 страниц | ~50-100 | $1.00 - $4.00 |
| 500 страниц | ~100-200 | $2.50 - $10.00 |

#### Поиск/Вопрос (каждый раз):

| Тип запроса | Вызовов API | Примерная стоимость |
|-------------|-------------|---------------------|
| Простой поиск | 1-2 | $0.01 - $0.10 |
| Сложный запрос | 2-3 | $0.02 - $0.20 |
| С генерацией ответа | 3-4 | $0.03 - $0.30 |

### Пример расчета для проекта:

**Сценарий:** Анализ 10 финансовых отчетов по 200 страниц каждый

1. **Индексация (один раз):**
   - 10 документов × $2.00 = **$20.00**

2. **Ежедневное использование (100 вопросов/день):**
   - 100 вопросов × $0.05 = **$5.00/день**
   - В месяц: **$150.00**

3. **Итого в месяц:**
   - Индексация: $20 (один раз)
   - Использование: $150
   - **Всего: ~$170/месяц**

---

## 💡 Как снизить стоимость?

### 1. **Кэширование индексов**

✅ **Индекс создается один раз** и сохраняется в JSON файл:
```python
# Индексация (один раз)
qa_system.index_document("document.pdf")
qa_system.save_index("document_structure.json")

# Загрузка сохраненного индекса (бесплатно)
qa_system.load_index("document_structure.json")
```

**Экономия:** Индексация делается один раз, а не при каждом использовании!

### 2. **Использование более дешевых моделей**

Можно изменить модель в коде:
```python
# Вместо GPT-4o использовать GPT-3.5-turbo
qa_system = DocumentQASystem(model="gpt-3.5-turbo")
```

⚠️ **Внимание:** Точность может снизиться, но стоимость уменьшится в 5-10 раз.

### 3. **Оптимизация параметров**

```python
# Уменьшить количество summary (меньше вызовов API)
opt = config(
    if_add_node_summary='no',  # Не генерировать summary
    if_add_doc_description='no'  # Не генерировать описание
)
```

### 4. **Локальные LLM** (требует модификации кода)

Можно адаптировать код для работы с локальными моделями:
- Ollama (Llama, Mistral)
- LM Studio
- vLLM

⚠️ **Сложность:** Требует значительной модификации кода.

---

## 🔄 Сравнение с традиционным RAG

| Аспект | Традиционный RAG | PageIndex |
|--------|-----------------|-----------|
| **Векторная БД** | Требуется (Pinecone, Weaviate) | ❌ Не требуется |
| **Embedding API** | Платно (OpenAI, Cohere) | ❌ Не требуется |
| **LLM для индексации** | ❌ Не требуется | ✅ Требуется |
| **LLM для поиска** | ❌ Не требуется | ✅ Требуется |
| **LLM для ответа** | ✅ Требуется | ✅ Требуется |
| **Общая стоимость** | Embedding + LLM | Только LLM |

**Вывод:** PageIndex может быть **дороже** для индексации, но **дешевле** для поиска (нет векторной БД).

---

## 📊 Рекомендации по использованию

### Для тестирования и разработки:

1. ✅ Используйте **бесплатные кредиты OpenAI** ($5)
2. ✅ Начинайте с **небольших документов** (10-50 страниц)
3. ✅ **Кэшируйте индексы** - не индексируйте повторно
4. ✅ Используйте **GPT-3.5-turbo** для тестов (дешевле)

### Для продакшена:

1. ✅ **Мониторинг расходов** - отслеживайте использование API
2. ✅ **Бюджеты OpenAI** - установите лимиты расходов
3. ✅ **Кэширование** - сохраняйте индексы в БД
4. ✅ **Оптимизация** - настройте параметры под ваши нужды
5. ✅ **Резервные модели** - имейте план на случай проблем с API

---

## 🆓 Бесплатные альтернативы

### 1. **Локальные LLM через Ollama**

```bash
# Установка Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Загрузка модели
ollama pull llama3.2
ollama pull mistral
```

⚠️ Требует модификации кода PageIndex для работы с локальными моделями.

### 2. **Open Source альтернативы**

- **LlamaIndex** - похожий подход, но с векторами
- **LangChain** - можно адаптировать под reasoning-based поиск

### 3. **Бесплатные API (ограниченные)**

- **Hugging Face Inference API** - бесплатный tier с лимитами
- **Groq** - быстрые бесплатные запросы (лимиты)

---

## 📝 Итоги

### ✅ Преимущества PageIndex:

- Высокая точность (98.7% на FinanceBench)
- Прозрачный поиск
- Не требует векторной БД
- Работает с документами любой длины

### ❌ Недостатки:

- **Требует платный OpenAI API**
- **Множественные вызовы API** при индексации
- **Зависимость от внешнего сервиса** (OpenAI)
- **Стоимость растет** с размером документа

### 💡 Рекомендация:

PageIndex отлично подходит для:
- ✅ Профессиональных приложений с бюджетом
- ✅ Критически важных задач (высокая точность)
- ✅ Документов, где важна прозрачность поиска

Может быть дорого для:
- ❌ Массового использования без бюджета
- ❌ Очень больших объемов документов
- ❌ Проектов без финансирования

---

*Обновлено: 2025*




