# ✅ Фронтенд готов к тестированию

## Статус

### Backend
- ✅ **Запущен** на порту 8000
- ✅ **Слушает** запросы
- ⚠️ Возможны проблемы с обработкой (502), но сервер работает

### Frontend
- ✅ **Запущен** на порту 5173
- ✅ **Готов** к работе

## Доступ к приложению

### Откройте в браузере:
```
http://localhost:5173/
```

## Тест индексации документа

### Через интерфейс:
1. Откройте http://localhost:5173/
2. Найдите раздел загрузки документов
3. Выберите PDF файл
4. Загрузите документ
5. Дождитесь завершения индексации

### Через API (альтернатива):
```bash
curl -X POST http://localhost:8000/api/documents/upload \
  -F "file=@путь/к/файлу.pdf"
```

## Проверка статуса

### Через интерфейс:
- Статус отображается в списке документов
- Обновляется автоматически

### Через API:
```bash
# Список всех документов
GET http://localhost:8000/api/documents/

# Статус конкретного документа
GET http://localhost:8000/api/documents/{id}
```

## Мониторинг

### Логи Backend:
```bash
tail -f backend/logs/backend.log
```

### Статусы документа:
- `uploading` - загрузка файла
- `indexing` - индексация в процессе
- `ready` - индексация завершена
- `error` - ошибка при индексации

## ⚠️ Важно

1. **Время индексации**: 10-30 минут для файла 8 МБ
2. **Не закрывайте браузер**: Процесс индексации идет в фоне
3. **Проверяйте логи**: При проблемах смотрите `backend/logs/backend.log`

## 🚀 Готово!

**Откройте http://localhost:5173/ и начните тест индексации!**

