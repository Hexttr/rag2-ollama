# ✅ Готовность к тесту индексации

## Статус серверов

### Backend
- **Порт**: 8000
- **Статус**: ✅ Запущен
- **URL**: http://localhost:8000
- **Health Check**: http://localhost:8000/api/health/

### Frontend
- **Порт**: 5173
- **Статус**: ✅ Запущен
- **URL**: http://localhost:5173/
- **Proxy**: Настроен для `/api` и `/ws`

## Готовность к тестированию

### ✅ Все готово для теста индексации документа!

**Что работает:**
1. ✅ Backend сервер запущен на порту 8000
2. ✅ Frontend сервер запущен на порту 5173
3. ✅ Proxy настроен для API запросов
4. ✅ Патчинг Ollama применен
5. ✅ API для документов готов

## Инструкция для теста

### 1. Откройте фронтенд
```
http://localhost:5173/
```

### 2. Загрузите PDF документ
- Используйте интерфейс загрузки документов
- Или отправьте POST запрос:
  ```bash
  curl -X POST http://localhost:8000/api/documents/upload \
    -F "file=@путь/к/файлу.pdf"
  ```

### 3. Проверяйте статус
- Через интерфейс фронтенда
- Или через API:
  ```bash
  GET http://localhost:8000/api/documents/{id}
  ```

### 4. Ожидайте завершения
- Статусы: `uploading` → `indexing` → `ready`
- Время: 10-30 минут для файла 8 МБ
- Мониторьте логи: `backend/logs/backend.log`

## Мониторинг

### Логи Backend
```bash
tail -f backend/logs/backend.log
```

### Проверка статуса через API
```bash
# Список всех документов
GET http://localhost:8000/api/documents/

# Статус конкретного документа
GET http://localhost:8000/api/documents/{id}
```

## ⚠️ Важно

1. **Время индексации**: Для больших файлов может занять 10-30 минут
2. **Статусы**: Документы проходят через `uploading` → `indexing` → `ready`
3. **Мониторинг**: Проверяйте логи регулярно
4. **Ollama**: Убедитесь, что Ollama запущен и доступен

## 🚀 Готово к тестированию!

**Откройте http://localhost:5173/ и начните тест!**


