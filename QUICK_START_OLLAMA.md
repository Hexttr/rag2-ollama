# 🦙 Быстрый старт: PageIndex + Ollama

## ✅ Да, можно использовать Ollama бесплатно!

PageIndex теперь работает с локальными моделями через Ollama.

---

## 🚀 Установка за 3 шага

### 1. Установите Ollama

**Windows:**
```bash
# Скачайте с https://ollama.com/download
# Или через PowerShell:
winget install Ollama.Ollama
```

**Linux/Mac:**
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### 2. Загрузите модель

```bash
# Рекомендуемая модель (хороший баланс):
ollama pull llama3.2

# Или более мощная:
ollama pull mistral
```

### 3. Запустите PageIndex с Ollama

```bash
# Убедитесь, что Ollama запущен
ollama serve

# В другом терминале:
python run_pageindex_ollama.py --pdf_path document.pdf
```

---

## 📝 Пример использования

```bash
# Базовое использование
python run_pageindex_ollama.py --pdf_path my_document.pdf

# С другой моделью
python run_pageindex_ollama.py --pdf_path my_document.pdf --model mistral

# С настройками
python run_pageindex_ollama.py \
    --pdf_path my_document.pdf \
    --model llama3.2 \
    --if-add-node-summary yes \
    --max-pages-per-node 10
```

---

## ⚙️ Настройка через .env

Создайте файл `.env`:

```bash
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.2
```

---

## 🎯 Рекомендуемые модели

| Модель | Команда | RAM | Качество |
|--------|---------|-----|----------|
| **llama3.2** | `ollama pull llama3.2` | 4GB | ⭐⭐⭐ |
| **mistral** | `ollama pull mistral` | 8GB | ⭐⭐⭐⭐ |
| **qwen2.5:14b** | `ollama pull qwen2.5:14b` | 16GB | ⭐⭐⭐⭐⭐ |

---

## ⚠️ Важно

1. **Скорость**: Локальные модели медленнее (10-30 мин для 100 страниц)
2. **Качество**: Может быть ниже, чем GPT-4o, но бесплатно!
3. **Память**: Убедитесь, что у вас достаточно RAM

---

## 📚 Подробная документация

См. файл **OLLAMA_INTEGRATION.md** для детальной информации.

---

*Наслаждайтесь бесплатным использованием! 🎉*






