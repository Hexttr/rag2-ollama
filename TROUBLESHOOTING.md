# 🔧 Устранение проблем

## Проблема: Загрузка документа не работает

### Проверка 1: Backend работает?

```bash
curl http://localhost:8000/api/health
```

Должен вернуть: `{"status":"healthy","service":"PageIndex Chat API"}`

### Проверка 2: База данных инициализирована?

```bash
cd backend
python -c "from app.database.database import init_db; init_db(); print('OK')"
```

### Проверка 3: Проверка загрузки через curl

```bash
curl -X POST http://localhost:8000/api/documents/upload \
  -F "file=@path/to/test.pdf"
```

### Проверка 4: Логи backend

Смотрите логи в терминале, где запущен backend. Должны быть сообщения:
- `Uploading file: ...`
- `File saved to: ...`
- `Document created with ID: ...`

### Проверка 5: Консоль браузера

Откройте DevTools (F12) → Console. Должны быть логи:
- `File selected: ...`
- `Starting upload...`
- `Document uploaded successfully` или ошибка

### Проверка 6: Network tab

В DevTools → Network проверьте запрос к `/api/documents/upload`:
- Status должен быть 200
- Response должен содержать `id`, `filename`, `status`

## Частые ошибки

### 1. CORS ошибка
**Решение:** Проверьте, что backend запущен и CORS настроен правильно

### 2. 500 Internal Server Error
**Решение:** 
- Проверьте логи backend
- Убедитесь, что БД инициализирована
- Проверьте, что директории `uploads/` и `indices/` существуют

### 3. Файл не загружается
**Решение:**
- Проверьте размер файла (макс 100MB)
- Убедитесь, что файл PDF
- Проверьте права доступа к директории uploads

### 4. Индексация не начинается
**Решение:**
- Проверьте, что Ollama запущен
- Проверьте логи backend на ошибки PageIndex
- Убедитесь, что модель Ollama загружена

## Логи для отладки

### Backend логи:
- `INFO` - нормальная работа
- `ERROR` - ошибки, требующие внимания
- `WARNING` - предупреждения

### Frontend логи:
- Откройте консоль браузера (F12)
- Смотрите ошибки в красном цвете
- Проверьте Network tab для запросов

---

*Если проблема не решена, проверьте логи и сообщите об ошибке*






